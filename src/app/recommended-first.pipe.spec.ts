import { RecommendedFirstPipe } from './recommended-first.pipe';

describe('RecommendedFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendedFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
