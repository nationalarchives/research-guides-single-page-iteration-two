import { GuidesWithKeywordPipe } from './guides-with-keyword.pipe';

describe('GuidesWithKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new GuidesWithKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
