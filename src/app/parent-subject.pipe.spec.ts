import { ParentSubjectPipe } from './parent-subject.pipe';

describe('ParentSubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
