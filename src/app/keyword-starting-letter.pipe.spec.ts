import { KeywordStartingLetterPipe } from './keyword-starting-letter.pipe';

describe('KeywordStartingLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeywordStartingLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
