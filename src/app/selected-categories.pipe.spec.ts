import { SelectedCategoriesPipe } from './selected-categories.pipe';

describe('SelectedCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
