import { NameSearchNumberFoundPipe } from './name-search-number-found.pipe';

describe('NameSearchNumberFoundPipe', () => {
  it('create an instance', () => {
    const pipe = new NameSearchNumberFoundPipe();
    expect(pipe).toBeTruthy();
  });
});
